[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "import_export_tool"
authors = [{name = "bickhsu", email = "bickhsu@gamil.com"}]
dynamic = ["version", "description"]
